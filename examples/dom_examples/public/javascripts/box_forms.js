window.addEventListener('load', function (event) {
  var red   = 0;
  var green = 0;
  var blue  = 0;

  var resultBox = document.querySelector('#resultBox');

  var redInput = document.querySelector('#redInput');


  // step 1.
    // select the #redInput
        // add an 'change' event handler to input field
        // change the boxes color to the specified red value

  // step 2.
    // Do the same as the above process for the #greenBox and #blueBox


  // step 3.
    // Change the result box color to rgb value specified in each box

});